# PostHog analytics — set this in Netlify environment variables
# Get your key from https://posthog.com → Project Settings → Project API Key
# Leave unset (or empty) for local development — analytics will not run
VITE_POSTHOG_KEY=
